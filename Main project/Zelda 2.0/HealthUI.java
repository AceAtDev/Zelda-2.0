import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class HealthUI here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class HealthUI extends UI
{
    /*
    

    private int xOffset = 15;
    
    private int amountOfHearts = 3;
    private int maxAmountOfHearts = 3;
    
    private GreenfootImage[] heartsMax = new GreenfootImage[maxAmountOfHearts];
    private GreenfootImage fullHeart = new GreenfootImage("Full Heart.png");
    private GreenfootImage emptyHeart = new GreenfootImage("Emtey Heart.png");

    int currentHp;
    
    public HealthUI(int xPos, int yPos, Link player)
    {
        super(xPos, yPos, player);
    }
    
    
    public void updateUi(int currentHealth)
    {
        
        if(currentHealth > amountOfHearts)
        {
            currentHealth = amountOfHearts;
        }
        
        for(int i = 0; i < heartsMax.length; i++)
        {
            if(i < currentHealth)
            {
                heartsMax[i] = fullHeart;
            }
            else
            {
                heartsMax[i] = emptyHeart;
            }
            
            if(i < amountOfHearts)
            {
                heartsMax[i].drawImage(heartsMax[i], xPos + (i * 2), yPos);
            }
            else
            {
                heartsMax[i].drawImage(heartsMax[i], xPos + (i * 2), yPos);
            }
        }        
    }
    */
    
}
